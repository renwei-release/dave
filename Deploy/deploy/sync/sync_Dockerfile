FROM fedora:30

ENV TZ=Asia/Shanghai
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

RUN yum update -y && yum install -y libgomp boost openssl protobuf grpc grpc-plugins cpprest

WORKDIR /
COPY ./file_system .

WORKDIR /project
CMD ["./dave-running.sh"]