# JNA 控制台项目

本项目为基于 Maven 管理的 Java 17 控制台程序，集成 JNA 库。

## 运行方法

1. 使用命令行进入项目根目录。
2. 执行 `mvn compile` 编译项目。
3. 执行 `mvn exec:java -Dexec.mainClass=Main` 运行主程序。

## 依赖
- Java 17
- Maven
- JNA 5.13.0

如需调用本地库，请参考 JNA 官方文档。
